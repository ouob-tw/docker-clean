# Docker Image 清理工具

版本：v1，使用者已於本次對話以「ACCPET」確認。

## Image 入口命名（2026-09-22 使用者直接授權）

- TUI 使用 `dcl image keep`（保留規則）與 `dcl image delete`（刪除選取項目）；標題包含 Docker Image Clean 及 Keep／Delete，說明不再使用白名單名稱。
- `dcl image clean` 維持自動化入口。`docker-clean keep`／`delete` 同樣可用，舊 `tui`／`whitelist` 僅保留為相容別名。

## 自動化 CLI（2026-09-22 使用者直接授權）

- 新增 `dcl image clean`；本節對此入口取代舊版不提供略過確認的限制，其餘入口維持原行為。
- `--delete REGEX` 只清理命中者，`--keep REGEX` 保護命中者；可重複、多條 OR、Python `re.search` 比對完整 tag，同 ID 任一 tag 命中即作用於整個 ID，保留優先。只有 keep 時清理其餘。
- 預設載入設定檔保留規則，命令列空白或無效 Regex 拒絕執行。delete 以 `None` 匹配無 tag image，keep 只匹配真實 tag。
- 預設只預覽，不詢問輸入；`--yes` 執行，`--force` 獨立控制強制刪除，預設跳過容器引用。按完整 ID 使用 `--no-prune` 刪除，包含全部 tag。
- 設定檔載入依下節；重用本機 socket 限制、逐項狀態重新檢查、狀態變動跳過、查詢失敗停止及個別失敗繼續規則。
- `--json` 輸出單一結果物件，含預覽與執行紀錄；規則或 Docker 錯誤輸出 JSON 錯誤。命令語法錯誤維持 stderr。結束碼 0 表示成功、預覽、無候選或狀態變動跳過；1 表示驗證或操作失敗；2 表示命令語法錯誤。

## Image 自動化設定檔（2026-09-23 使用者授權）

- 比照 container clean：`dcl image clean` 預設讀取 `~/.config/docker-clean/images.yaml`；`--config` 指定時只讀指定檔案。設定缺失、讀取或驗證失敗時停止，不因提供命令列規則而略過。
- 沿用 Image YAML 格式，只採用 `keep` 排除清單，與命令列 `--keep` 合併為 OR，保留優先於 `--delete`。不指定 `--delete` 時清理其餘未受保護 image；明確 `keep: []` 合法且文字預覽提示沒有保留規則。
- 不套用 YAML 的 force、remove_tags 或主題；`--yes`、`--force` 仍由命令列控制。不修改設定檔。
- 每項刪除前及重新盤點後檢查設定檔原始內容，變更、消失或讀取失敗即停止後續刪除並回傳失敗，保留已完成結果。
- JSON 的 keep 回報合併後規則。容器設定載入與 TUI 入口維持既有行為。

## Delete 刪除新流程（2026-09-21 使用者直接授權）

新增獨立入口 `dcl image delete`，以下規則適用新流程；本文其餘保留規則、YAML 與四種模式描述繼續適用原有 `keep`／`clean`。

- 初始不勾選任何 image，不讀寫舊版設定；Regex 與勾選不持久化。
- Delete 介面預設 E-Ink 白底黑字，游標／聚焦反相，停用按鈕以刪除線區別；Ctrl+T 可切換本次主題。
- `delete` 與 `keep` 的主題選單均提供 `e-ink`。`keep` 沿用主題保存機制，重新開啟仍套用，切換時不保存未完成的規則編輯；`NO_COLOR` 不移除 E-Ink 明確的黑白底色。
- 上方多行輸入框每行一個 Python Regex；按「Regex 勾選」後以 `re.search` 比對完整 tag，多條採 OR，命中就加入勾選。空白行忽略，無效 Regex 不改動選取。
- 輸入框下第一個按鈕為「Regex 篩選」，只顯示符合 Regex 的 image，不改變勾選；空輸入恢復全部，無效 Regex 保留前次篩選。無 tag 顯示 `None`，可用 `^None$` 篩選或 Regex 勾選，`.*` 也包含無 tag 項目；此名稱只用於顯示與比對，不新增實際 tag。兩者沿用 Python Regex 大小寫規則。
- 「Regex 篩選」旁提供「顯示全部」，只解除篩選，保留 Regex 文字及勾選；未套用篩選時停用。若從預覽操作，回到全部清單並使舊預覽失效，需重新預覽後確認。
- 表格支援 Space／Enter／滑鼠手動增減勾選。同一 image 任一 tag 命中即勾選整個 ID，全部 tag 一併列入刪除範圍；無 tag 可手動勾選。
- 預覽刷新資料，以表格顯示全部已勾選 image，不受瀏覽篩選限制；動作原因包含容器引用。可在預覽取消勾選或勾回原項目，確認只刪除仍勾選者。完整 ID 與全部 tag 可從「詳細資料」查看。零勾選不可確認；取消回到原篩選清單。修改 Regex、套用篩選或 Regex 勾選使預覽失效，調整終端大小不使預覽失效。
- 確認後逐項呼叫 `forceDeleteImage`，按完整 ID 使用 `--force --no-prune` 刪除，不修改容器或其他 Docker 資源。
- 每項執行前重新比對 image／tag／容器引用，變更就跳過，查詢失敗停止；個別刪除失敗可繼續下一項，不擴大確認名單。結果保留 Docker 實際回報，不保證 force 成功或釋放容量。
- 繼續遵守本機 socket 限制與競態限制；破壞性驗證只允許隔離 Engine。

## 問題與目標

主機累積大量 Docker image，需要保留特定映像並清理其餘映像。提供 TUI 管理 Regex 保留規則，設定以 YAML 存在本機；執行清理指令時，先預覽具體動作，再確認刪除。

## 範圍

以下為原 image 功能範圍；容器例外見「容器清理與設定分離」，image 自動化例外見「自動化 CLI」。

- 只操作 Docker image 及其 tag。
- 不刪除、停止、重啟或修改任何容器，包括已停止容器。
- 不清理 volume、network、build cache 或主機資料夾。
- 第一版不提供排程、背景清理或略過預覽確認的模式。
- 僅支援本機 Docker Engine；若連線指向遠端，停止清理並說明原因。

## 使用者流程

1. 執行 `dcl image keep`，列表依序顯示選取標記、tag、大小、建立日期、動作原因；完整 ID 與容器引用狀態保留在詳細預覽。
2. 新增、修改、刪除 Regex 規則，即時查看命中的 tag、受保護的 image 與原因。
3. 可從列表勾選 image 以產生跳脫後、加上首尾錨點的 tag 規則；持久化內容仍統一為 Regex。無 tag 的 image 不產生虛構名稱規則。
4. 選擇「移除 tag」與「強制刪除 image」，兩者初始預設皆關閉；儲存後，下次載入已儲存設定。
5. 從 TUI 進入預覽，或執行 `docker-clean clean` 使用已儲存設定。
6. 預覽列出保留、跳過、移除 tag、刪除 image 等具體動作及理由；確認才執行，取消不修改 Docker。
7. 完成後顯示成功、跳過及失敗項目與原因。

## 保留規則

- 唯一規則形式是 Regex，比對 Docker 回報的完整 `repository:tag` 字串，含 registry、port 及 namespace，不做模糊名稱別名轉換。
- 採搜尋匹配；需要精確匹配時使用 `^` 與 `$`。Regex 語法與實作選用的引擎必須在說明文件記載。
- 多條規則採 OR；同一 image ID 的任一 tag 命中，就保護整個 image 及所有 tag，所有清理模式均不得操作它。
- 無 tag 的 image 不以 `<none>:<none>` 作為真實名稱匹配；未受容器保護時列為清理候選，預覽清楚標示「無 tag」。
- `:` 是一般字元；例如 `^postgres:16$` 精確保留該 tag，`^postgres:` 保留該 repository 的所有 tag。版本中的字面小數點要跳脫。

## YAML 設定

預設位置為 `~/.config/docker-clean/images.yaml`。TUI 可儲存，CLI 與 TUI 使用同一份設定。

```yaml
theme: terminal
keep:
  - '^postgres:16$'
  - '^myorg/.*:.*$'

cleanup:
  remove_tags: false
  force: false
```

- 以上規則僅為範例，不自動套入使用者設定。
- `keep` 必須是字串陣列，清理選項必須是布林值；缺少選項時預設 false。
- `theme` 預設 `terminal`，使用終端原生 ANSI 配色；也可為內建主題名稱。Ctrl+T 切換主題後自動保存此欄位，不連帶儲存尚未完成的規則或清理選項修改。外部修改衝突仍拒絕覆蓋。
- 設定不存在時，TUI 可建立新設定；清理指令停止並提示先建立設定，不當成空白保留清單。
- YAML 解析、結構驗證、讀取或 Regex 編譯失敗時，停止整次清理，指出錯誤，不退回無保護模式。
- 明確的 `keep: []` 合法，但預覽須清楚顯示沒有保留規則。
- 儲存採完整驗證後原子替換；若設定自載入後被外部修改，要求重新載入，避免靜默覆蓋。

## 刪除語意

保留規則優先於下列全部選項。兩個勾選項分別控制「可否逐一移除 tag」與「Docker 刪除請求是否使用 force」，不得以 prune 取代經預覽的逐項操作。

| 移除 tag | 強制刪除 | 行為 |
|---|---|---|
| 關 | 關 | 排除被任何容器引用的 image；對其餘候選按 ID 正常刪除。多 tag 等 Docker 衝突不自動升級處理。 |
| 開 | 關 | 排除被任何容器引用的 image；對其餘候選逐一正常移除 tag，無 tag 者按 ID 正常刪除。最後一個 tag 移除時，Docker 可能同時刪除 image。 |
| 關 | 開 | 允許對未命中保留規則的候選按 ID 使用 force，包含有容器引用的候選；引用容器與 force 行為須列入預覽，由 Docker 決定是否可刪。 |
| 開 | 開 | 對未命中保留規則的候選逐一使用 force 移除 tag，無 tag 者按 ID 使用 force；容器引用仍列入預覽。 |

- 「移除 tag」不是保證只解除名稱而保留 image 資料的操作；最後一個引用消失時可能釋放資料。
- force 不代表停止或刪除容器，不保證 Docker 接受刪除，也不保證釋放底層資料。
- 執行時限制刪除作用於預覽目標，避免 Docker 預設遞迴清除未列入預覽的父映像。
- 顯示 image 大小供參考；共用層不可直接加總成保證可回收容量，untag 成功不等於資料已釋放。

## 預覽與執行一致性

- 預覽包含完整 image ID、全部 tag、引用容器名稱與狀態、保留原因、預計動作及 force 是否啟用。
- 修改規則或選項後，舊預覽失效，必須重新產生並確認。
- 執行前重新讀取設定與 Docker 狀態；設定變動、tag 改指向、新增保護 tag 或容器引用變動時，停止或跳過受影響項目並說明，不能擴大原先確認的名單。
- Docker 查詢失敗不視為沒有 image 或沒有容器引用。
- 逐項執行，不透過 shell 拼接使用者輸入；個別失敗不改用 force 重試，可繼續處理仍符合預覽的其他項目。
- 結果區分移除 tag、刪除 image、跳過與失敗；有失敗時 CLI 回傳非零狀態。
- Docker 不提供預覽到刪除間的完整交易鎖；force 仍有外部程序同時改動的競態限制，文件不得宣稱完全消除。

## 驗收與測試

下表定義驗收條件；實際版本、結果與證據統一記於 [QA 驗收紀錄](docs/qa/results.md)。以使用者可見結果為主，不以內部函式呼叫成功代替驗收。

| 編號 | 情境 | 預期結果 | 驗證方式 |
|---|---|---|---|
| A01 | TUI 建立、修改、刪除規則，儲存後重開 | YAML 與畫面一致，命中項目即時更新 | 真實終端 TUI 操作 |
| A02 | 精確 tag、repository 前綴、registry port、小數點及多規則 | 匹配符合上述語意 | 公開規劃入口測試及 TUI |
| A03 | 同一 ID 多 tag，僅其中一個命中 | 整個 image 與全部 tag 在四種模式均保護 | 規劃測試及隔離 Docker |
| A04 | 設定缺失、損壞、型別錯誤、無效 Regex、外部修改 | 明確錯誤；不刪除、不靜默覆蓋設定 | CLI/TUI 行為測試 |
| A05 | 兩選項四種組合；單 tag、多 tag、無 tag | 動作符合刪除表格，衝突不自動升級 force | 隔離 Docker |
| A06 | 運行中與已停止容器引用 image | 正常模式跳過；force 模式如實預覽並回報 Docker 結果，容器始終不被修改 | 隔離 Docker，操作前後比較容器 |
| A07 | 預覽取消、零候選、空保留規則 | 取消及零候選不刪除，空規則清楚提示 | 真實終端 TUI 及 CLI |
| A08 | 預覽後設定、tag 或引用改變 | 舊動作停止或跳過，不刪除新目標或新增保護項目 | 可控制狀態變化的行為測試 |
| A09 | Docker 無法連線、權限不足、個別刪除遭拒 | 明確錯誤與結果，不把失敗當成功 | CLI/TUI 及隔離 Docker |
| A10 | 執行完畢 | 區分 untag 與 deleted，共用層容量不誤報 | 隔離 Docker 與結果畫面 |
| A11 | 未列入預覽的父映像及非 image 資源 | 不被連帶清理 | 隔離 Docker |
| A12 | 目前連線為遠端 Engine | 清理停止並說明 | 連線選擇行為測試 |

測試切入點以「設定及 Docker 盤點資料 → 可見預覽 → 執行結果」為主；另以真實 TUI 操作驗證互動，使用隔離 Docker Engine 驗證真正的刪除語意。不能在主機既有映像上執行破壞性驗收。QA 後續補上需求獨立情境及整合後路徑、證據與 PASS／FAIL／UNPROVEN／NOT_EXECUTED 判定。

## 確認與後續

本版整理既有共識，並具體化四種選項組合、無 tag 處理與測試邊界。使用者已以「ACCPET」確認本版，依 LAT 自動拆票、實作、獨立審查及 QA；技術選型記於英文 PLAN。

### 使用者後續介面要求（已直接授權）

- 按鈕與選項框採單行精簡樣式。
- 欄位順序為 tag、大小、建立日期、動作原因，另保留窄的選取標記欄；不在表格重複完整 ID。
- 長 tag 自動換行；大小採 B／KiB／MiB／GiB 等人類可讀單位，建立日期精確到秒。
- Space、Enter、滑鼠皆可選取；勾選不可重設游標或捲動位置。
- 「移除 tag」改名「逐一移除標籤」：針對同一 image ID 的 tag，並非按相同 repository 名稱批量清除。
- 主題預設跟隨終端原生配色，使用者切換後存入 YAML 並於重啟時還原。

### 排序、捲動與執行進度（2026-09-21 已直接授權）

- 表格預設依 tag 字母升冪排序，無 tag 置後；點擊各欄標題切換升冪／降冪，大小與日期依實際數值排序。
- 勾選只更新受影響儲存格，保持當下列位置、游標與捲動位置；「選」與「動作原因」欄在再次點擊標題或重建清單時重新排序。
- 滾輪在任何內層捲動區（表格、Regex 輸入框、詳細紀錄）到達邊界後，不得傳給外層；內容不足、不需捲動時也一樣。正常內部捲動距離維持不變；滑鼠移到外層區域時才可捲動外層。
- E-Ink 主畫面使用純黑白，包含聚焦、停用控制項與進度區，不使用灰底或連續動畫。
- 確認後，盤點、刪除及最後重新盤點移到背景執行緒，介面保持可回應。執行期間鎖住規則、選項、勾選、確認及重新盤點，Ctrl+Q 等待工作結束才可離開；本版不提供中途取消。
- 進度按 image 計算，顯示已處理／總數、完成／跳過／失敗及目前名稱與階段；查詢失敗中止時保留實際進度，不把未執行項目算成完成。
- 進度與詳細紀錄顯示在懸浮視窗；紀錄按時間由舊到新、由上到下排列。同一 image 的 tag 回報合併顯示，保留 Docker 原始回報與完整 ID。
- 紀錄停在底部時跟進最新內容；往上閱讀時保留閱讀位置。最終重新盤點失敗須保留既有刪除紀錄並顯示錯誤。

### 精簡主畫面與懸浮視窗（2026-09-22 已直接授權）

- 原本兩行操作說明移入「使用說明」懸浮視窗，按鈕置於畫面最右上角。標題為靜態文字，點擊不展開；聚焦按鈕可用 Space／Enter 操作。「重新盤點」按鈕改名「刷新」。
- Delete 輸入框預設可輸入兩行內容，隨內容增高，最多四行；超過後在框內捲動，邊框不計入內容行數。
- 移除主畫面的外層捲軸。中央清單與預覽使用同一區域；Delete 維持表格，舊版保留規則模式維持完整文字預覽。
- 預覽、確認、取消等操作與快捷鍵固定於底部同一排，操作按鈕靠左，Ctrl+Q 與 Ctrl+P 靠右；Ctrl+T 保留功能但不顯示在頁尾。
- 確認刪除後開啟進度懸浮視窗；執行期間禁止關閉與取消，結束後可返回主清單。再次執行建立新的進度與紀錄，不混入前一次結果。

## 容器清理與設定分離（2026-09-22 使用者直接授權）

本節擴充原本僅操作 image 的範圍，僅 `dcl container clean` 可以刪除容器；既有 image 入口不改變容器。排程交由外部 cron，不新增背景服務，不自動安裝刪除排程。

- image 設定預設為 `~/.config/docker-clean/images.yaml`；不再自動回退到舊 `config.yaml`。明確 `--config` 始終使用指定檔案。`dcl image clean` 同樣載入此設定檔的 keep，與命令列規則合併。
- 容器使用獨立 `~/.config/docker-clean/containers.yaml`，可用 `--config` 指定；唯一規則來源為設定檔。必填 version: 1、正整數 stopped_days、keep。keep 支援 compose 與 container_names；缺省子清單為空，明確 keep: {} 合法且預覽警示無保留規則。
- compose 是規則陣列，每條必填 project、可選 services（非空字串陣列）。省略 services 保留整個 project；提供時保留該 project 內列出的所有 service 實例。services: [] 為錯誤。container_names 為名稱陣列。全部精確比對，任一命中即保留；不同於 image tag Regex。Compose 以 Docker labels 識別，不猜測名稱。
- 僅 status=exited 且最後 FinishedAt 距現在至少 stopped_days 天的容器符合。預設範例為 7 天，不以建立時間計算。created、dead、running、paused、restarting、removing 與含 Swarm task/service ID label 的容器一律跳過並說明。
- 日期缺失、零值、無時區、未來時間或無法解析時停止清理；設定缺失、未知欄位、型別錯誤、無效結構與查詢失敗同樣停止，不視為無保留規則。
- 預設只預覽；--yes 執行，--json 輸出單一物件，含模式、候選／保留原因、完整 ID、名稱、狀態、時間、labels、掛載、實際結果與錯誤。文字輸出提示可寫層損失與掛載保留。預覽先完成，才能逐項執行。
- 每項刪除前重讀設定、inspect 同一完整 ID，比對名稱、狀態、最後啟停時間、labels 與掛載。設定改變或查詢失敗停止；容器改變跳過。容器在 ls／inspect 之間消失也視為查詢失敗，停止本次清理，不自動重試。只呼叫 container rm ID，不帶 force 或 volumes，不清理 image/network/cache。匿名 volume 保留但不保證重新建立時自動掛回。
- 個別刪除失敗可繼續，保留已完成回報；退出碼 0=成功／預覽／無候選／狀態變動跳過，1=設定查詢操作失敗，2=命令語法錯誤。沒有 Docker 交易鎖，重新檢查不能消除所有競態。
- 每天 03:00 排程由使用者啟用 cron；flock 防止排程重疊，輸出導向紀錄。不得自動搭配 image prune。
- 驗證涵蓋精確保留與多副本、停止時間界線、Swarm／其他狀態、錯誤設定、設定變更與容器競態、image 預設路徑不回退及原功能回歸。主機只允許唯讀預覽；破壞性驗證使用隔離 Engine。

## CLI 改名（2026-09-22 使用者直接授權）

- 短指令改為 `dcl`，套件不再安裝 `dc`，避免與其他工具衝突。
- `docker-clean` 舊入口及設定目錄維持不變。

## 容器預覽精簡（2026-09-23 使用者授權）

- 預設文字只列刪除候選及其掛載，首行使用「名稱 | 原因 | 前 12 碼 ID」。其餘容器以保留、未滿期限、Swarm、其他狀態分類摘要呈現。
- 新增 --all，額外列出其他分類及原因，不展開非候選掛載。僅影響文字顯示，不改刪除範圍、順序、退出碼或 JSON；與 --json 同用時不影響 JSON。
- JSON 保留完整 entries，新增每項 category（candidate、keep、too_recent、swarm、state）、summary、stopped_days、plan_complete。摘要分類互斥；盤點不完整時明確標示，不宣稱沒有候選或完整合計。
- 完整盤點且無候選時顯示「沒有刪除候選」及摘要，不顯示刪除提醒或 --yes 提示。
- 有候選時提示：「刪除容器及內部檔案，保留掛載資料。」
- 執行模式先輸出預覽，再刪除；結束顯示刪除、跳過、失敗、未處理數量。查詢失敗中止時保留既有結果，未完成盤點時未處理數量標示未知。不修改原先清理安全規則。

## 移除舊設定路徑相容（2026-09-23 使用者授權）

- 本機重複的 `config.yaml` 經比對與 `images.yaml` 完全一致後移至垃圾桶，保留 `images.yaml` 原始內容。
- Image 預設路徑固定為 `~/.config/docker-clean/images.yaml`，即使只有舊檔也不自動載入；CLI 設定缺失照既有規則停止。
- 明確 `--config` 仍可指定任意檔案；容器設定與 CLI 相容別名不受影響。
