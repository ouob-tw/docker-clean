[Unit]
Description=Clean stopped Docker containers with Docker Clean

[Service]
Type=oneshot
Environment=PATH=/usr/local/bin:/usr/bin:/bin
ExecStart="%h/.local/bin/dcl" container clean --config "%h/.config/docker-clean/containers.yaml" --yes
